- **Shallow Copy:** Copies object **reference**, not nested objects.
    
- **Deep Copy:** Copies **actual objects** recursively.
    

Use:

- `clone()` → usually shallow.
    
- Custom deep copy → manual or with libraries (like serialization).