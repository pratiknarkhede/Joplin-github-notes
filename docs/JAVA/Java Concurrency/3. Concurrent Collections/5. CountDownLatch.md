&nbsp;

**Analogy:** Imagine a race organizer (the main thread) waiting to fire the starting gun. The gun can only be fired (`await()` finishes) after *all* the runners (worker threads) have signaled they are in position (`countDown()`). Once the gun fires (count reaches zero), the gate is open, and it stays open.

&nbsp;

&nbsp;