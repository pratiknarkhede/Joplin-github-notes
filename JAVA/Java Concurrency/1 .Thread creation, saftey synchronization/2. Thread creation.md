You can create threads in Java by:

- **Extending Thread class:** Override the **run(**) method to define thread behavior.
- **Implementing Runnable interface:** Preferred, as it allows your class to extend another, avoiding Java’s single inheritance limit.

&nbsp;

Creating thread by extending to Thread class

```java
class MyThread extends Thread {
    @Override
    public void run() {
        System.out.println("Thread running: " + Thread.currentThread().getName());
    }
}
```

&nbsp;By Implementing Runnable and overriding run() method

&nbsp;

```java
class MyRunnable implements Runnable {
    @Override
    public void run() {
        System.out.println("Runnable running: " + Thread.currentThread().getName());
    }
}
```

&nbsp;

so If we you see there is no actual implementation to handle thread creating in runnable class's run()

implementing run() means , we are simply providing logic for run() but no thread is created yet   
<br/><br/>To actually execute Runnable in the thread

we need to pass Runnable implementation for Thread constructor

```java
MyRunnable myRunnable=new MyRunnable();  
Thread myThread =new Thread(myRunnable)
thread.start()
```

&nbsp;now calling thread.start(), will initiate new thread, it will trigger native os kernel level thread creating via JVM  
<br/>the jvm schedules the new thread and calls run() internally;

by calls run() , I am referring to run() method in Thread class

&nbsp;

Generally thread creation is preferred as it will let the class to extend to another class, unlike Thread,  
where multiple inheritance will be the case.  
<br/><br/>Threads in java stops execution when their run() method execution is completed or any unhandled exception occurs  
or due to external interruption

The JVM handles resource cleanup.

&nbsp;

&nbsp;

&nbsp;